# MAXPROC público · Run 07 · trazabilidad temporal del postcheck
# Public MAXPROC · Run 07 · postcheck temporal traceability

**Fecha / Date:** 2026-08-23 06:39 CEST  
**Estado / Status:** `TARGET_PASS / WORKFLOW_VERIFICATION_PENDING`  
**Frontera / Frontier:** `NEOCore™ 7.3-CANDIDATE ≠ CANON`

## ES · Castellano

### Estado observado

El gate global de simetría vigente demuestra un estado limpio en el corte público más reciente observado antes de esta reparación:

- 309 Markdown activos;
- 244 documentos con secciones ES/EN divididas;
- 0 fallos estructurales divididos;
- 0 fallos de marcadores;
- 0 superficies pareadas pendientes;
- 0 plantillas de Issue con etiquetas visibles no simétricas.

El postcheck dinámico de README/índices/enlaces también figura `OK`, con 397 Markdown activos revisados, 17 README, 10.778 enlaces internos de ruta comprobados, 0 enlaces internos rotos y 0 fallos canónicos críticos.

### Problema elegido

El generador `.github/scripts/audit_markdown_links_readmes.py` regeneraba métricas dinámicas actuales pero escribía siempre una fecha fija:

`**Fecha / Date:** 2026-08-12`

Esto producía una contradicción de procedencia temporal: un informe vivo y regenerado en commits posteriores conservaba una fecha histórica fija, de modo que las métricas actuales podían interpretarse erróneamente como pertenecientes al corte del 12/08.

### Acción

Se corrigió exclusivamente el generador del postcheck:

- se añadió `datetime`/`timezone` de la biblioteca estándar;
- se calcula `run_date = datetime.now(timezone.utc).date().isoformat()`;
- la cabecera del informe usa ahora `f'**Fecha / Date:** {run_date}  '`;
- no se alteraron reglas del gate, exclusiones, recuentos, navegación, manifests, Neoaxiomas, Síntesis, canon ni contenido editorial.

**Commit de reparación:** `42f7b082d800822ac0dd7b2c7c173acc6a7ee528`.

### Pruebas y resultado

- Se verificó que el script anterior contenía literalmente la fecha fija `2026-08-12`.
- Se verificó que el workflow `audit-markdown-readmes-links` incluye `.github/scripts/audit_markdown_links_readmes.py` entre sus rutas de disparo y ejecuta el script antes de exigir `Estado = OK`.
- Se verificó que el script actualizado conserva toda la lógica de auditoría y sólo cambia la procedencia temporal de la fecha del reporte.
- La auditoría global ES/EN más reciente continúa en `0 / 0 / 0` y no se ha promovido 7.3-CANDIDATE.
- Al cierre de esta iteración todavía no existe un commit posterior verificable del bot que demuestre la regeneración del postcheck con la nueva fecha; por tanto, no se atribuye un PASS del workflow todavía.

**Resultado del objetivo:** `PASS`.  
**Resultado del workflow posterior:** `VERIFICATION_PENDING`.

### Residuos

No queda un defecto funcional conocido en la lógica del postcheck seleccionado. Queda únicamente verificar que la ejecución automática provocada por el cambio regenere el informe con fecha dinámica y conserve el estado documental `OK`.

### PASO_SIGUIENTE

**Verificar la ejecución del workflow disparada por `42f7b082...` y comprobar que el postcheck regenerado muestra fecha actual dinámica y mantiene 0 enlaces internos rotos, 0 fallos canónicos críticos y `Estado = OK`; sólo después continuar con el siguiente defecto material del corpus.**

---

## EN · English

### Observed state

The current global symmetry gate proves a clean state at the latest public cut observed before this repair:

- 309 active Markdown files;
- 244 documents with split ES/EN sections;
- 0 split structural failures;
- 0 marker failures;
- 0 paired surfaces pending review;
- 0 Issue templates with non-symmetric visible labels.

The dynamic README/index/link postcheck also reports `OK`, with 397 active Markdown files reviewed, 17 README files, 10,778 internal path links checked, 0 broken internal links and 0 critical canonical failures.

### Selected problem

The `.github/scripts/audit_markdown_links_readmes.py` generator regenerated current dynamic metrics while always writing a fixed date:

`**Fecha / Date:** 2026-08-12`

This created a temporal provenance contradiction: a living report regenerated by later commits retained a historical fixed date, so current metrics could be misread as belonging to the 12 August cut.

### Action

Only the postcheck generator was corrected:

- standard-library `datetime`/`timezone` were added;
- `run_date = datetime.now(timezone.utc).date().isoformat()` is computed;
- the report header now uses `f'**Fecha / Date:** {run_date}  '`;
- no gate rules, exclusions, counters, navigation, Manifestos, Neoaxioms, Synthesis, canon or editorial content were changed.

**Repair commit:** `42f7b082d800822ac0dd7b2c7c173acc6a7ee528`.

### Tests and result

- The previous script was verified to contain the literal fixed date `2026-08-12`.
- The `audit-markdown-readmes-links` workflow was verified to include `.github/scripts/audit_markdown_links_readmes.py` in its trigger paths and to execute the script before enforcing `Status = OK`.
- The updated script was verified to preserve the full audit logic and change only the temporal provenance of the report date.
- The latest global ES/EN audit remains at `0 / 0 / 0`, and 7.3-CANDIDATE has not been promoted.
- At iteration close there is still no later verifiable bot commit proving regeneration of the postcheck with the new date; therefore no workflow PASS is attributed yet.

**Target result:** `PASS`.  
**Post-change workflow result:** `VERIFICATION_PENDING`.

### Residues

No known functional defect remains in the selected postcheck logic. The only remaining item is to verify that the automatic execution triggered by the change regenerates the report with the dynamic date and preserves documentary status `OK`.

### NEXT_STEP

**Verify the workflow execution triggered by `42f7b082...` and confirm that the regenerated postcheck shows the current dynamic date while preserving 0 broken internal links, 0 critical canonical failures and `Status = OK`; only then move on to the next material corpus defect.**
