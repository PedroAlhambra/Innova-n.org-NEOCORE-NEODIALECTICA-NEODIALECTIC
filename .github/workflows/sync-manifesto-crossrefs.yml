name: sync-manifesto-crossrefs

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/sync-manifesto-crossrefs.yml'
      - '.github/scripts/sync_manifesto_crossrefs.py'
      - '.github/scripts/sync_manifesto_reference_policy.py'
      - '.github/scripts/sync_manifesto_index.py'
      - '.github/scripts/audit_manifesto_clickable_relations.py'
      - 'manifiestos/**.md'
      - 'neoaxiomas/README.md'
      - 'obras/idea/README.md'

permissions:
  contents: write

concurrency:
  group: neocore-document-sync-main
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Sync canonical cross-references and clickable relation surfaces
        run: python .github/scripts/sync_manifesto_crossrefs.py
      - name: Sync cross-reference policy
        run: python .github/scripts/sync_manifesto_reference_policy.py
      - name: Sync manifesto index and canonical frontier
        run: python .github/scripts/sync_manifesto_index.py
      - name: Generate canonical mirrors from material sources
        run: python .github/scripts/sync_canonical_manifestos.py
      - name: Audit canonical registry completeness
        run: python .github/scripts/audit_manifesto_registry_completeness.py
      - name: Audit NEOCore role ontology
        run: python .github/scripts/audit_neocore_role_ontology.py
      - name: Audit clickable manifesto relations and Open Synthesis issues
        run: python .github/scripts/audit_manifesto_clickable_relations.py
      - name: Commit synchronized manifesto navigation
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add manifiestos/*.md manifiestos/canonicos/*.md
          if git diff --cached --quiet; then
            echo 'Manifesto navigation already synchronized.'
            exit 0
          fi
          git commit -m 'docs: sincronizar navegación e índice de manifiestos'
          git pull --rebase origin main
          git push origin main
