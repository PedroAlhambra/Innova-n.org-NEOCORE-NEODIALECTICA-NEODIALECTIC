name: retired-one-shot-symmetrize-synthesis-readme

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired one-shot
        run: echo 'Retired after failed bootstrap; README symmetry is being corrected by direct traceable commits.'
